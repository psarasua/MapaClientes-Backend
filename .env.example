# Variables de entorno para MapaClientes Backend
# Copia este archivo como .env para desarrollo local

# Configuración básica
NODE_ENV=development

# Base de datos PostgreSQL
DATABASE_URL=postgresql://usuario:contraseña@host:puerto/database

# Puerto para desarrollo local (opcional)
PORT=3000

# CORS Origin (opcional, por defecto permite todas las origins)
CORS_ORIGIN=*

# Variables específicas para Fly.io (solo para referencia)
# Estas se configuran con 'fly secrets set'
# FLY_APP_NAME=mapclientes-backend
# FLY_REGION=mia
